spring:
  application:
    name: event-service
  lifecycle:
    timeout-per-shutdown-phase: 30s
  kafka:
    consumer:
      group-id: police-event-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: false
      observation-enabled: true
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      retries: 3
      acks: all
      observation-enabled: true

server:
  port: 8083
  shutdown: graceful
  tomcat:
    threads:
      max: ${SERVER_TOMCAT_MAX_THREADS:200}
      min-spare: ${SERVER_TOMCAT_MIN_SPARE_THREADS:20}
    max-connections: ${SERVER_TOMCAT_MAX_CONNECTIONS:8192}
    accept-count: ${SERVER_TOMCAT_ACCEPT_COUNT:100}

management:
  tracing:
    sampling:
      probability: 1.0
    propagation:
      type: w3c
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318/v1/traces}
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus,metrics,env,loggers,threaddump,heapdump,scheduledtasks"
  endpoint:
    health:
      probes:
        enabled: true
        add-additional-paths: true
      show-details: always
      group:
        liveness:
          include: livenessState,ping
        readiness:
          include: readinessState,redis
        startup:
          include: readinessState
    # Allow log-level changes at runtime without restart (safe for prod debugging)
    loggers:
      enabled: true
  # ── Tag every metric with service name + environment ──────────────────────
  # Visible in Prometheus as: http_server_requests_seconds{service="event-service",env="prod",...}
  metrics:
    tags:
      service: ${spring.application.name}
      env: ${spring.profiles.active:default}
    # ── Enable p50/p95/p99 histogram buckets for all HTTP endpoints ──────────
    distribution:
      percentiles-histogram:
        http.server.requests: true
        resilience4j.circuitbreaker.calls: true
      percentiles:
        http.server.requests: 0.5, 0.95, 0.99
        resilience4j.circuitbreaker.calls: 0.5, 0.95, 0.99
      # SLO buckets — "how many requests finished under 50ms / 100ms / 500ms?"
      slo:
        http.server.requests: 50ms, 100ms, 200ms, 500ms, 1s
    # ── Enable all JVM metrics (heap, GC, threads, classloader) ─────────────
    enable:
      jvm: true
      process: true
      system: true
      tomcat: true
      kafka: true
      hikaricp: true
      resilience4j: true

resilience4j:
  circuitbreaker:
    instances:
      redisReadOps:
        slidingWindowSize: 20
        minimumNumberOfCalls: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
      redisWriteOps:
        slidingWindowSize: 20
        minimumNumberOfCalls: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
  retry:
    instances:
      redisReadOps:
        maxAttempts: 3
        waitDuration: 200ms
      redisWriteOps:
        maxAttempts: 3
        waitDuration: 200ms
  ratelimiter:
    instances:
      redisReadOps:
        limitForPeriod: 300
        limitRefreshPeriod: 1s
        timeoutDuration: 0
  bulkhead:
    instances:
      redisReadOps:
        maxConcurrentCalls: 100
        maxWaitDuration: 0
      redisWriteOps:
        maxConcurrentCalls: 100
        maxWaitDuration: 0

police:
  event:
    idempotency:
      ttl: ${POLICE_EVENT_IDEMPOTENCY_TTL:PT24H}
  kafka:
    topics:
      telemetry: police-telemetry
      telemetry-retry: police-telemetry-retry
      telemetry-dlq: police-telemetry-dlq
    retry:
      main:
        attempts: 2
        backoff-ms: 1000
      retry-topic:
        attempts: 2
        backoff-ms: 2000
